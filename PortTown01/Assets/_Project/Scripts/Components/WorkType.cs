namespace PortTown01.Core
{
    public enum WorkType
    {
        Logging       = 0,
        Milling       = 1,
        Trading       = 2,
        DockLoading   = 3,
        // add new ones only at the end to avoid renumbering
        // FurnitureMaking = 4,
    }
}
